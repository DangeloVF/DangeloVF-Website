RewriteEngine On
RewriteCond %{HTTPS} off
RewriteCond %{HTTP_HOST} !^localhost [NC]
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Block direct access to sensitive files
<FilesMatch "^\.env|^\.env\.example|composer\.(json|lock)$">
    Order allow,deny
    Deny from all
</FilesMatch>
